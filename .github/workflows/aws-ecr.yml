name: Automate Building and Pushing Docker to AWS ECR

on:
  push:
    paths-ignore:
      - "docs/**"
      - "**.md**"
    branches: [main]

jobs:
  validate:
    uses: trussworks/shared-actions/.github/workflows/aws-ecr.yml@aae5a2c52d67c27a1ab11c348c4460ae09f21375 #v0.0.6
    secrets: inherit
    with:
      aws-region: "us-west-2"
