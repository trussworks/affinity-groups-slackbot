name: validate

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    uses: trussworks/shared-actions/.github/workflows/validate-python.yml@bd419488477c202a2ee405f1488da037a6f2c0a9 #v0.1.0
    with:
      python-version: "3.10"
